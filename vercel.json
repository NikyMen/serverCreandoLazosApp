{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "node ./node_modules/prisma/build/index.js generate && npm run build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}